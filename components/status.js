const boxStatus = {
    ACTIVE : "ACTIVE",
    SELECT : "SELECT",
    DEFAULT : "DEFAULT"
}

const letterStatus = {
    READ : "read",
    UNREAD : "unread"
}

module.exports = {
    box : boxStatus,
    letter: letterStatus
}